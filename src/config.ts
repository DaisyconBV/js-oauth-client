import {ConfigInterface} from 'src/config.interface';

export const config: ConfigInterface = {
	accessTokenUri: 'https://login.daisycon.com/oauth/access-token',
	authorizeUri: 'https://login.daisycon.com/oauth/authorize',
	cliRedirectUri: 'https://login.daisycon.com/oauth/cli',
};
